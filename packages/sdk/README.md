# @packages/sdk

This directory contains the SG Forge SDK v1. It provides postMessage communication interfaces for iframe client-side contexts, and client libraries for interacting with the Platform backend REST endpoints (token validation, directory context, hierarchy, permission checks).
